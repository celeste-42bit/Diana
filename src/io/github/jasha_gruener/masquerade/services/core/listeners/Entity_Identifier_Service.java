package io.github.jasha_gruener.masquerade.services.core.listeners;
//Output all messages with # in front into console!


//TODO THIS CODE IS READY! ACTIVATE AND TEST FUNCTION NEXT TIME A BUILD IS CREATED!
/*
import io.github.jasha_gruener.masquerade.main.Main;
import net.dv8tion.jda.api.entities.*;
import net.dv8tion.jda.api.events.message.MessageReceivedEvent;

public class Entity_Identifier_Service extends Main {
    public void onMessageReceived(MessageReceivedEvent event) {
        if (event.getAuthor().isBot()) return;

        User author = event.getAuthor();

        Message message = event.getMessage();
        String content = message.getContentRaw();
        MessageChannel channel = event.getChannel();
        Member member = event.getMember();
        Role role = event.getGuild().getPublicRole();

        System.out.println(content);
    }
}
*/